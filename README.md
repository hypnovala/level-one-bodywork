# level-one-bodywork

## Local development

Run the local server:

```bash
npm run dev
```

Then open `http://localhost:3000`.

Optional for local form submissions:

```bash
echo "RESEND_API_KEY=your_key_here" > .env.local
```

Quick build check:

```bash
npm run build
```
